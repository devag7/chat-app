export function ChatAppLogo({ className = "w-8 h-8" }) {
  return (
    <div className={`${className} flex items-center justify-center font-bold text-primary`}>
      <span>ChatApp</span>
    </div>
  );
}
